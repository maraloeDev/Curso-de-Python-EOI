def ejercicio05_4x4():
    N = 4
    tablero = [[0]*N for _ in range(N)]
    # X en posición (0,1) según la imagen
    tablero[0][1] = -1
    # Aquí iría la llamada a la función recursiva
    return tablero